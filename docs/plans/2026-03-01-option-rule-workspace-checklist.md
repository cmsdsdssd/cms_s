# 옵션 규칙 워크스페이스 완성 체크리스트

- [x] 축(옵션 카테고리) 단위 모드 선택(`Sync`/`Override`) UI 제공
- [x] 값(하위 옵션) 단위 전략 선택(`기본값(+0)`/`Sync`) UI 제공
- [x] 값 단위 룰 항목 선택 UI 제공(글로벌 등록 항목만 노출)
- [x] 값 단위 추가금(+/-) 입력 UI 제공
- [x] 옵션-규칙 작업 테이블에 `예상 옵션금액` 표시
- [x] 옵션-규칙 작업 테이블에 `예상 최종금액` 표시
- [x] R2 선택 시 자동 분류 경로(`소재 -> 카테고리 -> 중량범위`) 표시
- [x] Sync 적용 시 룰 라운딩 정보(`단위/모드`) 표시
- [x] Sync 적용 시 `sync_rule_set_id` 누락 방지
- [x] 재계산에서 Sync 델타 + 수동 추가금 동시 반영
- [x] 드로어 미리보기 결과를 드로어 내부에 표시
- [x] 드로어 재오픈 시 초안(드래프트) 보존
- [x] LSP 진단 확인
- [x] eslint 확인(에러 0, 기존 경고만 유지)
- [x] build 통과
